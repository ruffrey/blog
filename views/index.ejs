<h1>The Blog of Wiggleware</h1>
<br />
	
	<%
		for( var b=0; b< blogs.length; b++ )
		{
	%>	
		<div class="hero-unit">
			<h2>
				<a href="/blogs/<%=blogs[b].blogClassname.replace(/\_/g,'-')%>">
					<%=blogs[b].blogtitle%>
				</a>
			</h2>
			<div class="row-fluid">
				<div class="span6">
					<h6><%=blogs[b].pubdate%></h6>
				</div>
				
				<div class="span6">
					<h6 class="pull-right">by <%=blogs[b].author%></h6>
				</div>
				
			</div>
			 
			<div class="row-fluid">
				<div class="span12">
					<%-partial('blogs/'+blogs[b].blogClassname)%>
				</div>
			</div>
			<div class="row-fluid">
				<div class="span12">
				<h4>Tags</h4>
				<%
					if( blogs[b].tags && blogs[b].tags.length>0 )
					{
				%><h5>
				<%
						for( var t=0; t<blogs[b].tags.length; t++ )
						{
				%>		<a href="/search/<%=encodeURIComponent(blogs[b].tags[t])%>">
							<%=blogs[b].tags[t]%></a>&nbsp;
				<%			
						}
				%></h5>
				<%
					}
					else{
				%>	<p>None</p>
				<%
					}
				%>
				</div>
			</div>
		</div>
		
	<%	
		}		
	%>

